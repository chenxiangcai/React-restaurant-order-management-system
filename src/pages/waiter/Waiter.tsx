import React, { FunctionComponent } from 'react';

interface OwnProps {
}

type Props = OwnProps;

const Waiter: FunctionComponent<Props> = (props) => {

  return (
      <div>
        Waiter
      </div>
  );
};

export default Waiter;
